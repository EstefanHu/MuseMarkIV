import React from 'react';

const Actions = props => {
  return (
    <button style={ join }>
      Join Beta
    </button>
  )
}

const join = {
  border: 'none',
  borderRadius: '10px',
  height: '30px',
  width: '80px',
  fontWeight: 'bold',
  fontSize: '.8rem',
  color: 'white',
  backgroundColor: 'black'
}

export default Actions;